# 我们的宇宙：Codex × Grok Build 协作规则

所有 AI 执行者开始工作前必须阅读：

1. `README.md`
2. `docs/DEVELOPMENT_WORKFLOW.md`
3. `docs/CODEX_GROK_WORKFLOW.md`
4. 用户或路线图指定的当前活动任务 `docs/tasks/YYYY-MM-DD-*.md`
5. 任务涉及的新方案文档（入口：`docs/space-exploration/README.md`）

## 强制规则

- 这是独立的桌面 Web 项目，不是旧仓库的多端兼容层。
- 一个任务只使用一个任务文档作为规格、实现、验收和返修记录。
- Codex 负责任务分级、C1/C2 前置评审、架构边界和最终验收。
- Grok Build 只在任务文档 B 节允许后实现，严格遵守允许/禁止文件范围。
- Grok Build 的“完成”只表示“实现完成，等待 Codex 验收”，不代表任务通过。
- 两个代理不得同时编辑同一任务或同一文件；交接前必须停止写入并更新任务文档。
- 不覆盖、回退、清理或格式化任务外既有改动。
- 不得删除测试换取通过，不得沿用另一代理的历史测试数字。
- 唯一目标平台是桌面 Web；不开发、不构建、不测试移动端或微信小程序。
- 未经用户明确要求，不提交、推送、部署、发布、安装依赖或引入外部资产。
- 不把密钥、Cookie、临时 URL、本机绝对路径、私人照片或未授权音频写入仓库。
- 《星际穿越》音乐未取得完整授权前不得进入仓库或构建产物。
- 遇到 Blender 建模、拓扑/权重、骨骼绑定、动作设计与修复、复杂着色器或高难度 3D 架构任务时，开工前必须提醒用户切换到 GPT-6；未经用户确认，不直接进入专业制作阶段。

## 质量限制

- React 页面/业务组件 ≤ 300 行，超过 220 行应评估拆分。
- 通用工具文件 ≤ 200 行。
- Canvas/WebGL 渲染引擎 ≤ 400 行。
- 单个函数原则上 ≤ 60 行。
- Three.js 资源必须在取消、切页、隐藏和 destroy 后可靠释放。

## 默认协作路线

- G0：单代理完成并自查。
- G1 小任务：单代理直做；另一代理可按需抽查。
- G1 标准联合：Codex 规格 → Grok Build 实现/自测 → Codex 验收。
- C1：Codex 前审 → Grok Build 最小实现 → Codex 独立验收 → 必要时 Grok 最小返修。
- C2：Codex 主导诊断和核心实现；Grok Build 只承担已隔离的低风险配套。
