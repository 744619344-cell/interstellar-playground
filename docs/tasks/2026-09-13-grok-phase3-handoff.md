# Grok：P3 新资产索引、预览指南与只读核对

## A. 需求与验收标准

- 等级/路线：G0，纯文档，由 Grok Build 顺序完成三个子项。
- 状态：`accepted`。
- 用户因额度安排暂停 Codex 复杂开发，先分配低风险配套。P3-T06 仍由 Codex 保留，未开工；不得接手建模、裁剪、灯光或运行时接入。
- P3-T01 至 P3-T05 已验收；旧 `2026-09-12-grok-documentation-handoff.md` 已验收，不重做、不修改。此任务只补充后来新增的 P3 资产资料。

### 1. 新资产文档入口

更新 `docs/space-exploration/README.md`，增加现有 10–13 号资产登记和本轮新增指南的相对链接。将“当前只有 LOD2/LOD1，没有 LOD0”明确限定为星体运行时：飞船已存在正式 LOD0 资产，但尚未接入游戏。将已完成的 P3-T02 保留说明改为当前 P3-T06 由 Codex 保留；不重新校准整份路线图。

### 2. 编写可操作的预览指南

新增 `docs/space-exploration/14-SPACECRAFT-PREVIEW-GUIDE.md`。只读核查四个已有查看器的 HTML/JS，列出外部、内舱、机械、PBR 的开发服务路径、实际存在的按钮、LOD 和适用范围。给出外门开关、双门限制、控制条/喷焰、暂停/复位、LOD 切换和原材质对照的手动检查步骤及预期结果。

必须区分：指南不是实际执行结果，未进行浏览器操作就写“待人工执行”；已有报告只能引用来源和日期，不能冒充本轮测试。不把独立查看器说成游戏流程已接入。不添加不存在的功能或热键。

### 3. 只读核对 P3 资产与接入边界

新增 `docs/space-exploration/15-PHASE3-HANDOFF-CHECKLIST.md`。对现有 9 个飞船 GLB、6 张 PBR WebP 重新计算字节数与 SHA-256，逐项对照 10–13 号登记；记录相对路径、实测值、登记出处和一致/不一致结论。不得修改资产或原登记来消除差异；发现不一致记录并交回 Codex。

从现有源/登记提取供 P3-T06 接入时核对的清单：模块名、坐标约定、相机/座椅/EVA 锚点、六个动画名、材质及纹理颜色空间、纹理共享和释放的现有证据。只描述当前事实及待实现项，不设计新的 API、裁剪规则或灯光参数。不得将 WebP 文件大小当作 GPU 内存，也不得把 renderer 纹理统计值 1 写成 0；以 P3-T05 验收记录为准。

## B. Codex 前置授权与范围

允许 Grok 立即实现上述全部子项，不必逐项等待确认。开始时状态改为 `implementing`，完成后改为 `waiting_codex_review` 并停止写入。

唯一允许修改/新增：

- 本任务文档。
- `docs/space-exploration/README.md`（仅 A 节限定的入口与两处过时说明）。
- `docs/space-exploration/14-SPACECRAFT-PREVIEW-GUIDE.md`。
- `docs/space-exploration/15-PHASE3-HANDOFF-CHECKLIST.md`。

只读：README、开发工作流、任务记录、资产登记、源码、查看器和相关报告，以及仓库内资产文件。

禁止：其他文档、src、assets-source、模型/纹理/动画、脚本、测试、依赖/锁文件、配置、AGENTS.md；启动 Blender、修改业务或资产、安装依赖、引入外部资产、提交、推送、部署、清理文件、改系统 Git 配置。不得填写 D 节或自行判为 accepted。

验证：开工及交接读取 `git status --short --branch`，完成执行 `git diff --check`；直接检查未跟踪文件、Markdown 相对链接、按钮与源代码一致性、15 个资产的实测大小与哈希。Git 因所有权失败就如实记录，不修改全局配置、不冒称通过；空 diff 不代表未跟踪文件已检查。纯文档无需 npm 测试或构建，不引用历史数字声称本轮通过。

停止条件：某子项需要超出白名单、出现代码/资产缺陷或需要新工具时，在 C 节记录问题，继续其余不受影响的子项；不擅自修复任务外文件。

## C. Grok 实现与交接

### 执行信息

- 执行者：Grok Build
- 状态：`waiting_codex_review`
- 下一位：Codex 额度恢复后复核
- 未接手：P3-T06；未改 10–13 号登记、未重做 `2026-09-12-grok-documentation-handoff.md`

### 变更文件（仅白名单）

- `docs/space-exploration/README.md`：增加 10–15 号链接；星体运行时无 LOD0 的说明；P3-T06 由 Codex 保留
- `docs/space-exploration/14-SPACECRAFT-PREVIEW-GUIDE.md`（新增）
- `docs/space-exploration/15-PHASE3-HANDOFF-CHECKLIST.md`（新增）
- 本任务文档

### 子项 1

文档入口已接到 10–13 号登记、预览指南与接入清单。未重写整份路线图。

### 子项 2

指南覆盖四个查看器的开发路径、实际按钮、LOD 与不适用项。检查步骤全部标「待人工执行」。历史报告只引 2026-09-13 任务 D 节与 JSON 路径。未把查看器写成游戏已接入。源码无自定义热键。

### 子项 3

对 9 个 GLB、6 张 WebP 重新计算大小与 SHA-256，与 10–13 号登记 **15/15 一致**，无差异交回修复。接入清单摘自现有登记/源码：模块名、轴向、锚点、六个动画、sRGB/线性 ORM、查看器纹理共享与退出后统计值 1。未把 WebP 当 GPU 内存。

### 检查

- 开工/交接 `git status --short --branch`：`## No commits yet on master`；顶层未跟踪含 `assets-source/`、`docs/`
- `git diff --check`：exit 0；无已跟踪 diff，已直接读未跟踪新文档
- 本轮未跑 npm test/build，未引用历史通过数
- 未安装、提交、推送、部署；未改代码/模型/纹理/配置

### 未执行 / 风险

- 未在浏览器点击四个查看器
- P3-T06 与游戏接入仍未做
- 本轮只核磁盘文件整体哈希，未拆 PBR GLB 再比嵌入 WebP 块（登记声称嵌入一致）

### 实现者 → Codex

- 重点复核：1）入口链接与两处过时说明；2）指南按钮是否与 HTML 一致且未冒充实测；3）15 项哈希表是否与磁盘一致。

### 第 1 轮返修

- 返修轮次：`1/2`
- 问题：第 2.1 节将机械版概括为「不是更小的 GPU 方案」，未经实测且掩盖图集纹理成本
- 已改：`docs/space-exploration/15-PHASE3-HANDOFF-CHECKLIST.md` 第 2.1 节，改为机械版无图像纹理、PBR 降 draw call 但约 128 MiB 图集预算，整体 GPU 成本须场景实测
- 未改：D 节、其他文档、资产、P3-T06
- 状态：`waiting_codex_review`

## D. Codex 验收

### 第 1 轮复核：修改后复验（返修 1/2）

Codex 独立读取四份文档、查看器 HTML/JS、相关源与 GLB 元数据，重新计算磁盘文件：15/15 字节数与 SHA-256 均匹配清单和原登记；41 个 Markdown 相对链接均可解析。按钮、LOD、静态内门与机械动画区别、未实测声明、纹理统计值 1 与上下文释放的说明符合现有实现。未运行浏览器或 npm 测试/构建，本任务不需要。

本轮直接执行 Git 状态/差异检查受仓库所有权限制失败，未修改全局配置，不声称 diff 通过。已直接读取文档；当前缺乏已提交基线，不能仅凭实现者摘要证明全部任务外文件均未改动。

唯一需返修项：`15-PHASE3-HANDOFF-CHECKLIST.md` 第 2.1 节“机械版……不是更小的 GPU 方案”属于未经证实的整体 GPU 成本结论，且容易掩盖图集版新增纹理成本。直接读取 LOD0 元数据：mechanisms 为 0 图像 / 16 材质 / 45 网格，pbr 为 6 图像 / 3 材质 / 24 网格。P3-T05 记录的收益是绘制次数下降，代价包括约 128 MiB 图集预算；不能概括为整体 GPU 成本高低。

最小返修：将该句替换为“机械版用于原材质对照，不含图像纹理；PBR 版减少绘制次数，但新增约 128 MiB 的图集预算。整体 GPU 成本需在实际场景测量，不能仅凭版本或文件大小判断。”保留其余已通过内容。

返修仅允许修改该清单第 2.1 节及本任务 C 节/实现状态；不得修改 D 节、其他文件或资产。更新 C 节说明第 1 轮返修，状态回到 `waiting_codex_review` 后交回 Codex。P3-T06 保持未接手。

### 第 1 轮返修复验：通过

Codex 直接读取清单第 2.1 节与 C 节返修记录：未经证实的整体 GPU 成本结论已删除，替换内容准确区分机械版无图像纹理、PBR 减少绘制次数与新增约 128 MiB 图集预算，并明确整体成本须实际场景测量。C 节已记录返修 1/2；此前 D 节记录保留。

唯一结论：原复核的唯一返修项已关闭，本 G0 文档任务验收通过，状态 `accepted`。此前 15 项资产与 41 个链接的检查属于上一轮证据，本轮未重复运行或冒充新结果；未运行 npm 测试、构建或浏览器检查。P3-T06 仍由 Codex 保留，按用户额度安排暂缓，不在此次复验中启动。
